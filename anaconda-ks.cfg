# Generated by Anaconda 38.23.4
# Generated by pykickstart v3.47
#version=F38
# Use graphical install
graphical

# Keyboard layouts
keyboard --vckeymap=pl --xlayouts='pl'
# System language
lang pl_PL.UTF-8

# Repositories
url --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-38&arch=x86_64
repo --name=update --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f38&arch=x86_64

%packages
@^server-product-environment
docker
%end

# Run the Setup Agent on first boot
firstboot --enable

# Generated using Blivet version 3.7.1
ignoredisk --only-use=sda
autopart

# Partition clearing information
clearpart --all

# System timezone
timezone Europe/Warsaw --utc

# Root password
rootpw --iscrypted $y$j9T$6J7XdTzuJzjMpZNAZ5tYesNH$tJi0e6Eoxnw2yU8AXCrEWeYjICDD4ln.k3OyPIU9113

%post --erroronfail

cat << 'EOF' > /etc/systemd/system/my-docker.service

# Włączenie do grupy oraz uruchomienie usługi dockera
usermod -aG docker root
systemctl enable docker
systemctl start docker

cat << 'EOF' > /etc/systemd/system/runapp.service

[Unit]
Description=Pull image and run container
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=docker pull hek70r/snake
ExecStart=docker run -d -p 3000:3000 hek70r/snake

[Install]
WantedBy=multi-user.target
EOF

# Wlaczenie i uruchomienie zdefiniowanej uslugi
systemctl daemon-reload
systemctl enable my-docker.service
systemctl start my-docker.service

%end